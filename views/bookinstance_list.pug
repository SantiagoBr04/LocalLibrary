extends layout

block content
  h1= title
  
  if bookinstance_list.length
    ul
      each bookinstance in bookinstance_list
        li
          a(href=bookinstance.url)= bookinstance.book.title
  else
    p Nenhuma cópia encontrada.
