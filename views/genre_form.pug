extends layout

block content
  h1= title
  
  form(method='POST' action='')
    div.form-group
      label(for='name') Nome do Gênero:
      input#name.form-control(type='text', placeholder='Ex: Ficção Científica', name='name', required)
    
    button.btn.btn-primary(type='submit') Enviar
